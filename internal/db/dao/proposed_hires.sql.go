// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: proposed_hires.sql

package dao

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const approveProposedHire = `-- name: ApproveProposedHire :exec
UPDATE proposed_hires SET status = 'approved', reviewed_by = $2 WHERE id = $1
`

type ApproveProposedHireParams struct {
	ID         string  `json:"id"`
	ReviewedBy *string `json:"reviewed_by"`
}

func (q *Queries) ApproveProposedHire(ctx context.Context, arg ApproveProposedHireParams) error {
	_, err := q.db.Exec(ctx, approveProposedHire, arg.ID, arg.ReviewedBy)
	return err
}

const getProposedHire = `-- name: GetProposedHire :one
SELECT id, company_id, proposed_by, role, department, rationale, reports_to, status, reviewed_by, created_at FROM proposed_hires WHERE id = $1
`

func (q *Queries) GetProposedHire(ctx context.Context, id string) (ProposedHire, error) {
	row := q.db.QueryRow(ctx, getProposedHire, id)
	var i ProposedHire
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.ProposedBy,
		&i.Role,
		&i.Department,
		&i.Rationale,
		&i.ReportsTo,
		&i.Status,
		&i.ReviewedBy,
		&i.CreatedAt,
	)
	return i, err
}

const insertProposedHire = `-- name: InsertProposedHire :one
INSERT INTO proposed_hires (id, company_id, proposed_by, role, department, rationale, reports_to, status)
VALUES ($1, $2, $3, $4, $5, $6, $7, 'pending')
RETURNING id, company_id, proposed_by, role, department, rationale, reports_to, status, reviewed_by, created_at
`

type InsertProposedHireParams struct {
	ID         string  `json:"id"`
	CompanyID  string  `json:"company_id"`
	ProposedBy string  `json:"proposed_by"`
	Role       string  `json:"role"`
	Department *string `json:"department"`
	Rationale  *string `json:"rationale"`
	ReportsTo  *string `json:"reports_to"`
}

func (q *Queries) InsertProposedHire(ctx context.Context, arg InsertProposedHireParams) (ProposedHire, error) {
	row := q.db.QueryRow(ctx, insertProposedHire,
		arg.ID,
		arg.CompanyID,
		arg.ProposedBy,
		arg.Role,
		arg.Department,
		arg.Rationale,
		arg.ReportsTo,
	)
	var i ProposedHire
	err := row.Scan(
		&i.ID,
		&i.CompanyID,
		&i.ProposedBy,
		&i.Role,
		&i.Department,
		&i.Rationale,
		&i.ReportsTo,
		&i.Status,
		&i.ReviewedBy,
		&i.CreatedAt,
	)
	return i, err
}

const listPendingHiresByCompany = `-- name: ListPendingHiresByCompany :many
SELECT ph.id, ph.company_id, ph.proposed_by, ph.role, ph.department, ph.rationale, ph.reports_to, ph.status, ph.reviewed_by, ph.created_at, COALESCE(e.name, e.role, ph.proposed_by) as proposer_name
FROM proposed_hires ph
LEFT JOIN employees e ON e.id = ph.proposed_by
WHERE ph.company_id = $1 AND ph.status = 'pending'
ORDER BY ph.created_at ASC
`

type ListPendingHiresByCompanyRow struct {
	ID           string             `json:"id"`
	CompanyID    string             `json:"company_id"`
	ProposedBy   string             `json:"proposed_by"`
	Role         string             `json:"role"`
	Department   *string            `json:"department"`
	Rationale    *string            `json:"rationale"`
	ReportsTo    *string            `json:"reports_to"`
	Status       string             `json:"status"`
	ReviewedBy   *string            `json:"reviewed_by"`
	CreatedAt    pgtype.Timestamptz `json:"created_at"`
	ProposerName string             `json:"proposer_name"`
}

func (q *Queries) ListPendingHiresByCompany(ctx context.Context, companyID string) ([]ListPendingHiresByCompanyRow, error) {
	rows, err := q.db.Query(ctx, listPendingHiresByCompany, companyID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPendingHiresByCompanyRow{}
	for rows.Next() {
		var i ListPendingHiresByCompanyRow
		if err := rows.Scan(
			&i.ID,
			&i.CompanyID,
			&i.ProposedBy,
			&i.Role,
			&i.Department,
			&i.Rationale,
			&i.ReportsTo,
			&i.Status,
			&i.ReviewedBy,
			&i.CreatedAt,
			&i.ProposerName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listProposedHiresByCompany = `-- name: ListProposedHiresByCompany :many
SELECT ph.id, ph.company_id, ph.proposed_by, ph.role, ph.department, ph.rationale, ph.reports_to, ph.status, ph.reviewed_by, ph.created_at, COALESCE(e.name, e.role, ph.proposed_by) as proposer_name
FROM proposed_hires ph
LEFT JOIN employees e ON e.id = ph.proposed_by
WHERE ph.company_id = $1
ORDER BY ph.created_at DESC
`

type ListProposedHiresByCompanyRow struct {
	ID           string             `json:"id"`
	CompanyID    string             `json:"company_id"`
	ProposedBy   string             `json:"proposed_by"`
	Role         string             `json:"role"`
	Department   *string            `json:"department"`
	Rationale    *string            `json:"rationale"`
	ReportsTo    *string            `json:"reports_to"`
	Status       string             `json:"status"`
	ReviewedBy   *string            `json:"reviewed_by"`
	CreatedAt    pgtype.Timestamptz `json:"created_at"`
	ProposerName string             `json:"proposer_name"`
}

func (q *Queries) ListProposedHiresByCompany(ctx context.Context, companyID string) ([]ListProposedHiresByCompanyRow, error) {
	rows, err := q.db.Query(ctx, listProposedHiresByCompany, companyID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListProposedHiresByCompanyRow{}
	for rows.Next() {
		var i ListProposedHiresByCompanyRow
		if err := rows.Scan(
			&i.ID,
			&i.CompanyID,
			&i.ProposedBy,
			&i.Role,
			&i.Department,
			&i.Rationale,
			&i.ReportsTo,
			&i.Status,
			&i.ReviewedBy,
			&i.CreatedAt,
			&i.ProposerName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const rejectProposedHire = `-- name: RejectProposedHire :exec
UPDATE proposed_hires SET status = 'rejected', reviewed_by = $2 WHERE id = $1
`

type RejectProposedHireParams struct {
	ID         string  `json:"id"`
	ReviewedBy *string `json:"reviewed_by"`
}

func (q *Queries) RejectProposedHire(ctx context.Context, arg RejectProposedHireParams) error {
	_, err := q.db.Exec(ctx, rejectProposedHire, arg.ID, arg.ReviewedBy)
	return err
}
